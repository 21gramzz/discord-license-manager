import { LicensesTable } from '../components/LicensesTable';
import { ContentLayout } from '../../../components/Layout';

export const Licenses = () => {
  return (
    <ContentLayout padding="1rem 1.5rem">
      <LicensesTable />
    </ContentLayout>
  );
};
